import Nav from "./Nav";

const Header = () => {
    return (
        <header className="header">
            <h1 className="header__title">E-Commerce</h1>
            <Nav />
        </header>
    );
};

export default Header;

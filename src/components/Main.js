import Container from "./Container";
import ItemListContainer from "./ItemListContainer";

const Main = (props) => {
    const name = "Franco";

    return (
        <>
            <ItemListContainer />
        </>
    );
};

export default Main;

const ItemDetail = ({ item }) => {
    if (item)
    return (
        <div className="itemDetail">
            <header>{}</header>
            <main>
                <section>
                    <img src="" alt="" />
                </section>
            </main>
        </div>
    );
};

export default ItemDetail;
